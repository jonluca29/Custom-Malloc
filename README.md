# Custom-Malloc
A custom malloc implementation that utilizes mmap() for allocating, a free list for small deallocations and munmap() for large deallocations (>128 KB). This is an education project to demonstrate and learn dynamic memory allocation.
